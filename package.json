{
  "name": "kick-the-can-webapp-microservice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy:dev": "serverless deploy -v --aws-profile kickthecan:dev --stage dev",
    "deploy:prod": "serverless deploy -v --aws-profile kickthecan:prod --stage prod",
    "deploy:prod:ci": "serverless config credentials --provider aws --key '$AWS_KEY' --secret '$AWS_SECRET' --stage dev",
    "logs.account.dev": "serverless logs --function account --stage dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0"
  }
}
