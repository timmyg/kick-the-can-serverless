{
  "name": "kick-the-can-webapp-microservice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "creds:config:dev": "serverless config credentials --provider aws --key '$AWS_KEY_DEV' --secret '$AWS_SECRET_DEV'",
    "creds:config:prod": "serverless config credentials --provider aws --key '$AWS_KEY_PROD' --secret '$AWS_SECRET_PROD'",
    "deploy:dev": "serverless deploy -v --stage dev",
    "deploy:prod": "serverless deploy -v --stage prod",
    "logs.account.dev": "serverless logs --function account --stage dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {}
}
